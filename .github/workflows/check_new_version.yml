on:
  schedule:
    - cron: "30 8 * * */6"
  workflow_dispatch:



jobs:
  check_new_version:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
      - name: Check new version
        run: ./check_new_version.sh